# Hoist Prisma packages to the top-level node_modules so that
# Next.js Turbopack and TypeScript can resolve the generated client
# and its transitive runtime dependencies. This is the standard
# workaround for Prisma + pnpm + Next.js issues.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/*
